type Query {
  hello: String!
  notes: [Note]
}

type Mutation {
  addNote(text: String!): Note
  createAccount(username: String!, password: String!): Token
}

type Token {
  token: String!
}

type Note {
  id: ID!
  text: String!
}
